@model IEnumerable<Boutissante_Issam_TDI201_B_TR2_V2.Association>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ville.Nom_Ville)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Nam_Ass, "Details", new { id=item.Id_Ass }) |
        </td>
    </tr>
}

</table>
