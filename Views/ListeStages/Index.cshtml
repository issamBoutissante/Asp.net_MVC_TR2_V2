@model IEnumerable<Boutissante_Issam_TDI201_B_TR2_V2.Stage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Association.Nam_Ass)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date_Debut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date_Fin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Association.Nam_Ass)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date_Debut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date_Fin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Stage }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Stage }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Stage })
        </td>
    </tr>
}

</table>
